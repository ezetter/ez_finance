<% provide(:title, @account.name) %>
<div class="row">
    <aside class="col-md-4">
        <section class="user_info">
            <h1>
                <%= @account.name %> $<%= @account.value %>
            </h1>
        </section>
    </aside>
</div>
